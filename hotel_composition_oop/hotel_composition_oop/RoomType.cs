public enum RoomType
{ 
    // If guestNumber == n (roomType = some of the types based on number
    Single,
    Double,
    Triple,
    Quad,
    Queen,
    King,
    Twin,
    DoubleDouble,
    Studio
}